import React from 'react'

import { Markdown } from '../../Markdown'

export const TouchableHighlightExample0 = () => {
  return (
    <Markdown>
      {
        'Wrapper around [TouchableHighlight](https://reactnative.dev/docs/touchablehighlight)'
      }
    </Markdown>
  )
}
