import React from 'react'

import { Markdown } from '../../Markdown'

export const ActivityIndicatorExample0 = () => {
  return (
    <Markdown>
      {
        'Wrapper around [ActivityIndicator](https://reactnative.dev/docs/activityindicator)'
      }
    </Markdown>
  )
}
