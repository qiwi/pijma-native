import React from 'react'

import { Markdown } from '../../Markdown'

export const TouchableOpacityExample0 = () => {
  return (
    <Markdown>
      {
        'Wrapper around [TouchableOpacity](https://reactnative.dev/docs/touchableopacity)'
      }
    </Markdown>
  )
}
